When running the app, sometimes the current month day is not resolved correctly. It is often a day behind.

The expectation is that:

1. When the app launches the current day is resolved. Try to use the local system timezone if possible.
2. If someone leaves the app open overnight, the day gets incremented automatically.